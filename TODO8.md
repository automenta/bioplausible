# TODO8.md — Consolidated Plan

> **Rev 2026-08-31 (d).** P0–P5 session logs consolidated away (full history in `git log`).
> Research catalog lives in [RESEARCH3.md](RESEARCH3.md); this doc owns the engineering that unblocks it.
>
> **State:** P0–P5 **complete incl. pyright policy** · R1 **complete incl. construction seeding**
> (device threading, placement guard, runner auto-device, EqProp CUDA epoch ≈ 5.6 s) · U-bypass
> sweep complete · **R5.1a/b/c campaigns commissioned** (kill→resume lifecycles, golden manifests,
> 72/72 + 48/48 replication) · **R2 signal-honesty + zoo retirement + registry unification complete** ·
> **R5.3/5.4/5.5 complete** (quickstart truth-checked, CLI polish, PT2 export pin) ·
> **R5b-0 fidelity gate + retrospective audit complete** (imp-20/21/22/24 instrument repairs:
> manifest 8/72 → 48/72) · **R5b-A pre-registration + R5b-B locked campaign complete** ·
> **R5b-C defect-filtered evidence chain complete 2026-08-31 (rev d)**: imp-17 diagnosed and fixed —
> campaign episodes recorded latency-only resources, so energy/compute were constant 0 and the
> Pareto collapsed to one point; episodes now record a deterministic MAC proxy, parameter memory,
> target-free settled energy, and ψ-capacity — re-commissioned R5b-B shows a **25-coordinate
> frontier (429/480 dominated, hypervolume 412)** — plus **task-rotation parity fix** (visit-count
> family alternation; replication gate 0/48 → **48/48**) · **R5b-D discovery locks complete**:
> `core/campaign/discovery.py` + `tests/property/test_discovery_locks.py` — ① winner replication,
> ② attribution rank, ③ GPU replay, ④ fidelity standing; the registered R5b-B/C state is a
> **null claim** (top pooled effect plasticity null→fast_weights +0.024 is not stratified-stable
> at the 0.05 claimable floor — sign-flipping across (seed, family) strata) ·
> **R5.2/R5b-E closed** (one-liner documented; golden manifest fields verified) ·
> gate `pytest -q` green (1237 passed / 0 failed / 0 xpassed), pyright ontology 0 errors ·
> **Next: R5b-F Stage 1 report UI** (static HTML/JSON snapshot-tested), then RESEARCH3 handoff (R6);
> R3.4–3.8 and R4 remain pull-based.
>
> **Policy:** zero backwards compatibility · GPU-first for all training paths · no new tests for broken
> capability (xfail with precise reasons) · serial pytest only (xdist hangs in this env) ·
> **the System's own ParameterUpdate owns Δθ — external torch optimizers must not drive composed systems**
> (custom-loss harnesses route through `core.pipeline.apply_autograd_update`) ·
> **no scientific conclusion is drawn from any campaign delta until both arms of the comparison pass an
> implementation-fidelity check. Deltas involving known-defective or unvalidated axes are quarantined from
> attribution, not interpreted. A failed fidelity check is *inconclusive*, never a refutation of the hypothesis.**

## ✅ Completed Record (2026-08-30/31)

| Phase | Outcome |
|-------|---------|
| P0 | Registry lazy auto-population (28 native + aliases), KB constructor, PARAM_UPDATE registrations, module boundary — 1455 passed / 0 failed |
| P1 | `_TaskTrainer` rewrite, EWC consolidate, ModelAdapter real BPTT/metrics, geometry flattening — every triage failure fixed, none left as xfail |
| P2 | Native smoke 28/28 (pass/xfail w/ reasons), settle protocol 24+ pass, validation skips → xfails, 4 xpassed resolved, property locks green |
| P3 | Quarantine emptied (2 deleted, 6 enabled), free-energy tracking implemented, gRPC worker API drift fixed, 5 timeout victims fixed, parity flake root-caused — 1499 passed / 0 failed |
| P4 | `SubstrateSettleKernel` ported; `EnergyMinimizationDynamics.settle` substrate-native (Digital bitwise-equal to legacy); 10-test equivalence suite |
| P5 | Campaign schema freeze (migrations + `SchemaVersionError`), replication gate, counterfactual attribution, `CampaignStack` facade, 8 rankable objectives, migration smoke in CI, CLI validated end-to-end |
| P5 close | `pyrightconfig.json` (basic repo-wide, elevated-standard on `computronium/ontology`: 0 errors) + pre-commit hook `uv run pyright computronium/ontology`. Fixed 11 latent NameErrors in `ontology/system.py` (unimported substrate classes / `ComponentMetadata`), `GradientCredit` protocol conformance, `_settle_kernel` optional-subscript. Dead code deleted: `ontology/utils/state.py`, `ontology/dynamics/primitives.py`, 9 dead state helpers in `_dynamics.py`, duplicate `_layer_stack`/`_recurrent_weight`/`_learnable_weight_names`/`_set_param_name` copies, `_AdaptedSystem` dead `apply_pseudo_gradients` |
| R1 | `device` explicit on all native factories (unknown kwargs now raise), `compose_system`/`compose_joint_system`/`build_coordinate_system` take `device`; `_ComposedSystem.to()` **was a silent no-op** (dict reassignment never moved module params) — fixed via `nn.Module.to`; `device` property on both system shapes; runner auto-device (`AutoScientist._execute_proposal`, `CampaignStack` incl. checkpoints, `evaluate_episode` batch placement, `evaluate_migration` + joint suites via `get_device`); ψ init (`RoutingPlasticity`/`FastWeightPlasticity`) device-aware; placement guard over all 28 factories; EqProp MNIST epoch ≈ 5.6 s on CUDA |
| U-sweep | External-optimizer audit of every `torch.optim.*`/`create_optimizer` site: 4 ontology violations fixed (tradeoff ×2, hardware [18a], application [21c]), 2 dead strays deleted (core_tracks), 8 fake `use_spectral_norm`/`max_steps` kwargs removed (silently ignored → SN "ablations" compared identical models); new primitive `core.pipeline.apply_autograd_update` for custom-loss harnesses |
| R1.4 | Suite-wide construction seeding for the parity classes (`construction_seed()` helper in `test_ontology_parity.py`; applied to the 4 presets-vs-native pairs + threshold-bearing credit composition). Parity file green with seeded inits: 30 passed / 1 skipped / 2 xfailed — all parity thresholds hold |
| R5.1a | CPU smoke campaign commissioned via `scripts/commission_smoke_campaign.py`: mid-flight SIGKILL (process group) at 1 durable episode of iteration 1 → CLI `--resume` → completed through iteration 6, 13 episodes. Artifacts in `autoscientist_campaigns/smoke_cpu/records/`: `manifest.json` (git commit, torch/CUDA, seed, budget, kill/resume timeline), `report.md` (episodes, Pareto frontier, counterfactual attribution, replication gate), `run_first.txt`/`run_resume.txt`, YAML checkpoint copy. DB + `checkpoints/` gitignored by design |
| R5.1b | **GPU quick campaign commissioned 2026-08-31** — 3 seeds (0/1/2) × `quick_gpu{,_seed1,_seed2}`/`campaign-id quick_r51b_s*`: 4 coords/iter, 8 iterations, **33 episodes each**, kill@1-episode → resume lifecycle (~5.5 s + ~7.5 s per seed on RTX 3080). Manifest records `device_requested=cuda`/`cuda_available=true`; metrics/resources recorded; report renders frontier + counterfactuals + replication table. **Speedup caveat:** at smoke scale (8-dim synthetic, batch 16) episodes are kernel-launch bound — GPU ≈ CPU latency or worse per coordinate (measured from artifacts; e.g. recurrent/energy_minimization 2.2 ms CPU vs 9.3 ms GPU). Speedup evidence remains training-scale (EqProp MNIST epoch ≈ 5.6 s CUDA). Multi-seed via separate output dirs (script assumes one campaign per DB) — fold into R5.1c script extension if convenient |
| R2.4 | **xpass split complete 2026-08-31.** Native smoke tile tests split by measured ground truth (param-move probe): `tile_fa/tp/hebbian/pc` crash-free → strict-pass smoke; new `test_native_tile_learning_capability` locks learning signal (params must move after `train_step`) as strict xfail — all four leave 17/17 params frozen (the old xfail reasons were substantively right about learning, wrong about crash-freedom). `tile_ep/gnn` crash (`Energy-based settling requires a layered geometry` → candidate invalid coordinate, R3.9), `tile_snn` crash (tensor 16 vs 212 → implementation bug, R3.4) — precise strict xfails. Gate now: 0 xpassed |
| R2.5 | **Skip census complete 2026-08-31** (one pass). Dead legacy deleted: `tests/integration/test_algorithms_integration.py` (Sprint-7 removals), `tests/integration/test_triton_integration.py` (placeholder-only), `tests/unit/test_verify_bias.py` (`_MODEL_SPECS` gone), `TestC_SurrogateLocks` shells in `test_ontology_locks.py`, placeholder `test_equilibrium_gradients_match_bptt_looped_mlp` (native gradient equivalence already covered by `test_gradient_equivalence.py`), dead `test_feedback_alignment_eqprop` in `test_validation_all.py`. Skips→xfails: 5× `EnergyToInstantaneousAdapter modifies frozen config` (adapter bug, strict; includes J6 lock) — capability recovered: 5× `test_substrate_with_riemannian_update` un-skipped and passing on all substrates (the "known limitations" were stale). Legitimate residuals: axis_probe exhausted/pairwise-fenced (34), DEFERRED geometry (11), research-directions `build()` conditional (9), adapter coverage-gap params (7), biology/scaling conditional weight-lookup (5), optional deps (wandb/Triton-CPU) (4). Skip count now meaningful: 66→54 in gate |
| R2.6 | `uv add kademlia` (main dep — p2p DHT skips now exercise); `test_grpc_seam.py` rewritten: dead `test_grpc_seam_multi_process` stub + orphaned server scaffolding deleted (real-transport multi-process seam lives in `test_grpc_seam_subprocess.py`, slow-marked); fault-injection contract pinned locally. axis_probe `[2-0]` flake: no recurrence observed this session — still watching |
| R5.3 | Quickstart truth-checked **end-to-end on CUDA**: Backprop 93.3% / FF 92.9% @ 3 epochs (45 s / 66 s) — matches documented expectations; dead never-called `train_backprop`/`train_forward_forward` helpers deleted; `EPOCHS` env knob (`QUICKSTART_EPOCHS`, default 3) added for smoke use. `demo/main.py` compiles clean against current API (719-line NiceGUI app, still zero campaign wiring — R5b-F Stage 2). Slow-tier smoke added: `tests/slow/test_quickstart_smoke.py` (1-epoch subprocess run, asserts output contract) |
| R5.4 | `comp` prog-name fixed: `prog="biopl …"` → `comp …` across all CLI adapters; docstring/help `biopl` references normalized (module-entry commands point at `python -m computronium.cli.export_kernel` etc.); README "0 errors in strict mode" claim aligned with actual policy (elevated-standard on ontology, repo-wide basic). cli/ ruff diagnostics 268→264 (net negative) |
| R5.5 | PT2 export round-trip pin: `tests/integration/test_pt2_export_roundtrip.py` — FeedforwardGeometry (backprop) + RecurrentGeometry (EqProp) export via `deployment.export_to_pt2`, reload with `torch.export.load`, outputs bitwise-equal to eager on CPU. Note: `_ComposedSystem` is not an `nn.Module` — the geometry module is the export unit |
| R3-adjacent | **`core/construction.py` reflection bug fixed**: `resolve_consumption` reflected on `model_cls.__init__` — for plain function factories that resolves to `object.__init__`, reporting a spurious `**kwargs` catch-all, so hyperopt trials forwarded stale sampled kwargs (`learning_rate`, `max_steps`, …) into native factories and crashed against R1's kwargs rejection. Fixed via `_construction_callable` (reflect on the function itself); `tests/integration/test_hyperopt_integration.py` + `test_phase2_integration.py` now pass (were failing outside the gate) |
| Campaign determinism | **Improvement-10 + 11 fixed 2026-08-31.** (10) resume dedup: `run_campaign` snapshots recorded `(iteration, coordinate, task)` rows on resume and re-proposed slots emit `already_recorded` instead of re-executing; the crash-between-`add_episode`-and-`update_iteration` race can no longer duplicate rows (pinned by `test_resume_skips_already_recorded_episodes`; verified live: seed-0 kill→resume lands exactly 144/144 episodes). (11) per-episode construction seeding: θ init now derives from `(seed, campaign_id, iteration, coordinate)` via blake2b (`episode_seed`) and is applied with `torch.manual_seed` before composition — rebuilds replay identically (pinned by `test_rebuild_replays_identical_metrics`, the CPU-scale template for the R5b-D replay lock). Also: `FrontierRecord.seed` is now stamped from the campaign seed (was always the dataclass default 42 → the replication gate could never count seeds); `evaluate_episode` takes `seed` + passes `task_name` into `episode_batch`; redo path uses `checkpoint.task_name` instead of `task_cycle[0]` |
| Batch families + grid | `episode_batch` is task-family aware: `synthetic` (legacy stream pinned `1000+episode` — R5.1a/b artifacts reproduce) and new `parity` (nonlinear sign-bit sum; independent blake2b stream); unknown families raise (a real-dataset label can no longer masquerade as smoke data). New `space_grid` + `grid_sampler` (stateless round-robin over a sorted grid, keyed by the `(iteration, experiment)` slot — resume- and multi-seed-safe) + `CoordinateSampler` Protocol (positional-only slots) replacing the Callable alias; `comp campaign run --layout grid` and the 72-coordinate `joint_grid` search space. Task assignment rotates `(experiment + iteration) % len(tasks)` so a coordinate's repeat visits cover both families |
| R5.1c | **Commissioned replication campaign 2026-08-31** — `autoscientist_campaigns/r51c/`: `joint_grid` (72 coords) × 5 seeds × 2 task families, 8 coords/iter × 18 iterations (two full grid passes per seed; the +9-iteration repeat flips the task), 8-dim/batch-16 smoke scale, CUDA. Per-seed stores `seed_0..4/` (one campaign per seed, same script invocation — the multi-seed/multi-family script extension R5.1c asked for). Seed 0: kill@1-episode → resume → **exactly 144 episodes, zero duplicates**; seeds 1-4 clean (9.3 s each). Result: **720/720 episodes persisted, 72/72 coordinates replicated** (≥5 seeds × ≥2 families). Golden manifest (git commit, config hash, uv.lock hash, torch/CUDA, determinism flags, budget, per-seed detail, replication summary) + report + merged `episodes.json` + per-seed YAML checkpoints in `records/`. Headline deltas from the raw attribution (D-axis +0.82, M-axis +0.018) were **subsequently classified defect-driven by the R5b-0 retrospective audit — see next row; neither is interpretable** |
| R5b-0 audit | **Retrospective implementation-fidelity audit of the R5.1c deltas 2026-08-31** (diagnosis, not interpretation). Gate: `core/campaign/fidelity.py` probes over the 72-coordinate grid → **8/72 pass, 64 quarantined** (`r51c/records/fidelity_report.md` + `fidelity_manifest.json`). Verdicts: **(1) D-axis `instantaneous → energy_minimization` (+0.82) = defect-driven.** The instantaneous arm is inert — `InstantaneousDynamics.settle` ignores `target`, so free ≡ nudged ⇒ contrastive pseudo-gradients are structurally zero ⇒ Δθ = 0.00e+00 exactly; campaign mean acc 0.1226 ≈ 1/8 chance across all three credits. The energy arm's reported "accuracy" is **supervision leakage**: it is computed on the target-nudged settled state — `local_goodness` coords show acc 0.9635 with Δθ = 0.00e+00 (stub credit) and free-settle acc 0.1302 (chance); `random_projections` moves Δθ ≈ 5.7e-06 (near-inert). The +0.82 compares a leaky metric against an inert arm — it measures settle-to-target leakage vs chance, not "settling beats single-pass". (The known `EnergyToInstantaneousAdapter` frozen-config xfail is NOT in the campaign path — composition is direct; the defects are intrinsic to the D×C composition and the metric protocol.) **(2) M-axis `null → fast_weights` (+0.018) = defect-driven (inert instrument).** `run_train_step` takes no M-axis: `plasticity.step` is never invoked and `initial_psi`'s ψ is discarded, so ψ cannot modulate activity or credit — null/routing/fast_weights arms are functionally identical and the delta is θ-init noise (construction seed varies per coordinate). **(3) Passing subspace:** 8 coordinates (energy_minimization × null × {thermodynamic_contrast, random_projections} × 2 geometries × 2 updates); pooled C/U/G deltas within it (+0.036 credit, +0.014 update, +0.011 geometry) do **not** rank stably across the 10 seed×family strata — no claimable effect. Feed the passing set as the R5b-B grid. Neither verdict concludes any effect is real or fake — both trials are *inconclusive until the instrument is fixed* |
| imp-20 (metric leakage) | **Fixed 2026-08-31**: `run_train_step` adds post-update target-free `free_forward` pass → records `free_loss`, `free_energy`, `free_accuracy`. Legacy `accuracy` keeps name but documented as nudged-settle fit. `evaluate_episode` stamps `free_accuracy` as `task_accuracy` (honest learning metric), legacy `nudged_fit_accuracy` in metadata. Lock test: `TestMetricHonesty.test_free_accuracy_is_not_supervision_leaked` — inert-credit coordinate (RP feedback_scale=0) shows free_accuracy ≈ chance while nudged accuracy leaks. Fidelity gate manifest unchanged (8/72) — imp-20 fixes metric semantics but doesn't widen the credit/plasticity manifest. |
| imp-21 (credit bodies) | **Fixed 2026-08-31**: Implemented `compute_pseudo_gradient` for LocalGoodnessCredit (per-layer goodness via autograd), TemporalTraceCredit (rate-coded STDP surrogate; `compute_stdp_window` untouched), TargetInversionCredit (transpose-feedback target propagation via shared `_propagate_targets` helper), HomeostaticCredit (activity-setpoint scaling; new `homeostatic_target` config field). Axis certification surrogate-alignment tests xfailed (they FD the surrogate over manually-computed phases whose autograd graphs are disconnected — rewrite needed to run phases through `run_train_step`'s grad_ctx). Step 8b gate: **10/72** (feedforward/local_goodness × 2 updates added; recurrent/local_goodness fails honestly — EM settle does not preserve the autograd graph) |
| R3.1 (diffusion autograd) | **Fixed 2026-08-31**: `DiffusionDynamics.settle` now seeds each Langevin step with a fresh `requires_grad_` leaf and computes the internal `autograd.grad(energy, h)` under `torch.enable_grad()` — the internal gradient must run even when the pipeline holds `no_grad` for non-autograd credits (the original bug: first step worked only by accident of ambient graph state; subsequent steps crashed on detached `h`). `tests/integration/test_diffusion_integration.py` xfails removed — 4/4 pass; un-xfails the smoke entry. Note: no dynamics probe for diffusion yet, so diffusion coordinates stay "blocked" in the fidelity manifest (Execution Order 9 adds probes) |
| imp-22 (plasticity wiring) | **Fixed 2026-08-31**: ψ is threaded through the episode pipeline — `run_train_step` takes optional `plasticity`/`psi`/`context`, steps ψ once per episode via `plasticity.step(psi, z, context)` (z.activity carries x,y) and applies `plasticity.modulate(acts, psi)` per settled phase when the primitive declares it (Routing: per-sample sigmoid gate strength; FastWeight: per-sample additive contribution, truncate/pad to layer width; substrate_coupled/rule_state: engagement-only). `JointSystem.train_step` passes ψ instead of discarding `initial_psi`'s result. J2/J3 preserved (θ untouched intra-episode; ψ mutates only via `step`). M-probe verdict flipped: null = ψ const (0 steps, pass); routing/fast_weights = ψ stepped + engaged (pass). **Manifest widened to 30/72** (all EM × {null, routing, fast_weights} × {thermo, RP} × both geometries + feedforward/local_goodness variants). **Latent bug surfaced by engagement**: `RuleStatePlasticity.step` concatenated the raw input (8-dim) against a controller built for `operator_dim` (64) — never fired while `step` was unreachable; fixed by projecting x to the declared slot width (truncate/pad, FA-style). Pinned by axis_probe `[4-3]` |
| imp-24 (Execution Order 9) | **Complete 2026-08-31** — R3.2/R3.9 reclassification + probe coverage. **(a) D×C fence**: `instantaneous × thermodynamic_contrast` raises `IncompatibleCoordinateError` (subclass of `UnsupportedCoordinateError`, so campaign runners keep the skip path) from `evaluation._check_pairwise`; the gate records it as a coordinate-level fail verdict with the pairing + reason. Fixable nudge gaps (predictive_settling) deliberately NOT fenced — the dynamics probe quarantines them with repair-shaped verdicts instead. **(b) FA repair**: `RandomProjectionsCredit` rebuilt — top error δ_L = ∂L/∂logits via autograd, propagated down through FIXED random feedback matrices (`δ_i = δ_{i+1} @ B_i`, `B ~ feedback_scale·N(0,1)`, fixed at first use), `ΔW_i = δ_{i+1}ᵀ a_i / batch`; recurrent self-connections project the last hidden layer's error; ragged (tile) weights that break the layered contract yield zeros, never fabricated signal. Before the repair the flagship `create_fa_mlp`/`create_fa_system` had Δθ = 0.0 **exactly** (every FA "training" run inert — the loss-direction is now verified descending). `requires_autograd` flipped True (pipeline pin updated); `test_native_fa_mlp` un-xfailed; native-FA learning lock added. **(c) Dynamics probes** (fidelity.py, one helper per value dispatched via `_DYNAMICS_PROBES`): spike_integration = finite + membrane-bounded + spike-counts + deterministic + target-blind-by-spec (pass); predictive_settling = deterministic but does NOT descend its own tracked prediction-error energy (141.4 → 146.0) AND nudge-unwired (fail ×2, fix shape in detail); diffusion = finite + Langevin descent + noise present + nudge-unwired via **seed-locked free-vs-nudged comparison** (identical noise ⇒ identical output ⟺ target-blind; flake-free) (fail, fix shape). **(d) recurrent×local_goodness under EM — repaired, not fenced**: the EM settle graph DOES reach θ on recurrent geometry (AddBackward0 verified); the real blocker was the credits' `len(weight_names) != len(acts) − 1 → zeros` count-guard (recurrent geometry: 3 weights, 2 act transitions). All layered credits now map weights to act pairs positionally (`_weight_acts` with shape-contract validation → None pairs for tile/ragged weights) — LocalGoodness takes one autograd over the total goodness objective (recurrent weight included), TemporalTrace/Homeostatic/TargetInversion emit honest zeros for surplus/ragged weights. TODO8's earlier "EM settle does not preserve the autograd graph" was a misdiagnosis. **(e) M-probe upgraded (9.v)**: non-null plasticity must step ψ **and change it** (non-const) and, when a `modulate` hook exists, produce ψ-sensitive activity (zeroed-ψ ≠ stepped-ψ). Caught a live wiring bug on first run: the pipeline stepped ψ with `z.activity = {"x": x}` only — the FastWeight Hebbian branch (requires x AND y) was silent-dead; fixed to `{"x": x, "y": y}` (imp-22's record already claimed x,y — the probe caught the drift, exactly the imp-23 lesson). **(f) Surrogate-alignment locks un-xfailed (9.iv)**: FD closure re-runs the phase settling per perturbation under the pipeline's grad_ctx (`_settle_phases_grad`); convergence early-exit disabled in the alignment system (a ±eps FD pair can cross the exit step — a gradient through a step-count discontinuity is meaningless); FD support bounded (24 pts) with **support-restricted cosine** (unperturbed zeros dilute full-vector cosines — the old 0.0 cosines were partly this artifact); construction seeding pins θ-init (R1.4 lesson). LocalGoodness cos ≈ 1.0 (tol 0.90, EM dynamics); TargetInversion 0.95-0.99, un-xfailed at tol 0.90 with the residual documented (pseudo-gradient treats propagated targets as constants; the FD surrogate re-derives them from perturbed W). **(g) Dispatch loudness**: `compose_joint_system_from_configs` silent `else` fallbacks removed — unknown dynamics/update/plasticity types now raise; **"diffusion" coordinates had been silently running InstantaneousDynamics** (found because the direct-class dynamics probe and the composed-system credit probe disagreed); diffusion branch added. Default `_SMOKE_SPACE` + CLI `joint_smoke` credits: thermodynamic_contrast → local_goodness (all cross-product coordinates compose). **Manifest 30/72 → 48/72** (all 36 EM coords + 12 instantaneous×FA); r51c re-filter: 480/720 records survive. Post-repair stratified attribution: **±0.8 dynamics delta stable across all 10 strata — observed, NOT interpreted (R5b-A must pre-register it or kill it)**. Gate 1260 passed / 0 failed / 0 xpassed; pyright ontology 0 errors; integration 218 passed |

Old P6 checklist items already satisfied: EqProp anchor 81.32% MNIST · ComputroniumLinear (26 tests) · torch.jit → torch.export migration in `deployment.py`.

| R2.1 (zoo retirement) | **`computronium/zoo/**` deleted 2026-08-31** per the prepped extraction list. **Extracted:** (a) `ModelSpec`/`get_model_spec`/`load_weights` → `computronium/core/model_spec.py` (dead `get_propagators_for_model`/`get_optimizers_for_propagator` dropped — zero consumers; `load_weights` consolidated onto canonical `core.checkpoint.load_checkpoint`, dual-format: `Checkpoint` TypedDict *or* raw state-dict file); (b) `zoo/mep/**` → `computronium/mep/**` (presets, strategies, CUDA kernels, `_registration.py` — `muon_backprop`/`smep`/`smep_fast` lazy routes + PARAM_UPDATE/CREDIT_ASSIGNMENT registrations intact, verified live); (c) `TileLM` → `computronium/models/tile_lm.py`; (d) `zoo/models/deployments/**` → `computronium/models/deployments/**` (`DeploymentConfig` consumed by `config/experiment.py`). **Deleted outright (dead/superseded):** `zoo/propagators/` (canonical home is `core/local_learning/rules`), `zoo/sparsity/` + `zoo/utils.py` + `zoo/nebc_base.py` (only self-tests consumed), `zoo/optimizers/{standard,ewc,spectral}.py` (sgd/adam/adamw/rmsprop already registered natively in `ontology/optimizers.py` — pure duplicates; EWC canonical in `core/ewc.py` + ontology `ElasticConsolidationUpdate`; SpectralConstraint superseded by `SpectralConstrainedUpdate`), `zoo/models/{tile_models,tile_fa}.py` (superseded by native `native_tile_*`), their 4 test files, and broken `scripts/inventory_eqprop.py` (imported nonexistent `zoo.models.eqprop`). **Consumers re-pointed (14 modules + 8 test files):** `__init__.py` lazy table, `execution/{_guards,robustness}.py`, `hyperopt/{comparison,experiment,optuna_bridge,search_space}.py`, `config/experiment.py`, `benchmarks/{rigorous,compare_nanoGPT}.py`, `core/audit.py` (`_load_registry` triggers native via `Registry.list()` + imports `computronium.mep`; enumerates 101 components), `tests/property/_support.py`, `cli/shared.py` docstrings, `demo/runner.py` comment. Gate after: **1219 passed / 0 failed** (−41 = deleted dead tests), pyright ontology + new modules 0 errors |
| R2.3 (registry unification) | **Done 2026-08-31.** Module-level `list_models()` delegates to `Registry.list(ComponentCategory.MODEL)` — canonical view incl. aliases, lazy-trigger semantics consistent with every other Registry read (was: raw `_components` read that silently skipped aliases and bypassed `_ensure_native_registered`). Module-boundary test probe hardened: asserts raw `_components` empty **and** `computronium.models.native` absent from `sys.modules` after a light import (stronger than the old `list_models()` probe, which delegation would have self-fulfilled). Side fix: latent Hypothesis deadline flake in `test_registry_roundtrip.py` (`deadline=None`) — first native registration ≈1.2 s vs the 200 ms default deadline, exposed by test ordering |
| R5b-C (evidence chain) | **Complete 2026-08-31 (rev d)** — defect-filtered Pareto over the resource vector 𝒞 + knee attribution, both rendered by `scripts/fidelity_gate_report.py` over fidelity-passing records only. **(1) imp-17 diagnosed & fixed**: `evaluate_episode` recorded wall-clock latency only ⇒ energy/compute constant 0, `stability_score` saturated (ρ≡1.0 by PR-5 calibration, `lyapunov_local` hardcoded 0.0, settling_time = guard window) ⇒ Pareto degenerated to single-objective task_loss (1 frontier point, hypervolume 0). *Instrumentation bug, not a finding.* Wired `_episode_resources`: deterministic MAC proxy (params × batch × settled phases × settle steps), fp32 param memory, the episode's own target-free settled energy (`free_energy` metric), ψ-capacity from `plasticity.config.plastic_state_dims` (no RNG side effects — never re-derive `initial_psi`). **(2) Task-rotation parity fix**: family assignment was `(experiment + iteration) % len(tasks)` — every coordinate stayed on ONE family whenever the grid cycle was even (48/8=6), so the R5b-B replication gate read 0/48. Now visit-count-based (`visits[coordinate] % len(tasks)` derived from durable rows + in-run recordings — replay-safe across resume); resume dedup keys on `(iteration, coordinate)` (the task family is a deterministic function of the slot's visit state). **(3) Re-commissioned R5b-B**: 480 episodes (240+240), kill→resume on seed 0, **48/48 replicated** (≥5 seeds × ≥2 families), fidelity 48/48 pass / 480/480 survive, **Pareto 25 frontier coordinates / 429 dominated / hypervolume 412** (data-derived reference). Pooled top attribution (canonical, direction-merged): plasticity fast_weights→null −0.0243 — **not stratified-stable** (sign-flips across seed×family strata; e.g. dynamics ±0.2188 across parity strata). No claimable effect — consistent with the registered null. |
| R5b-D (discovery locks) | **Complete 2026-08-31 (rev d)** — `core/campaign/discovery.py` + `tests/property/test_discovery_locks.py` (17 tests). Spec-driven: `DiscoverySpec` (effect claim = axis transition + min_delta + metric; or `claim="null"`). **① winner replication**: the claimed gap must appear in every (seed, family) stratum ≥ min_delta; null claims hold while NO transition is stratified-stable. **② attribution rank**: claimed transition ranks first pooled (defect-filtered) and per-stratum; null claims hold while the pooled top is sub-threshold or strata disagree. **③ GPU replay**: `verify_replay` rebuilds an episode twice from `episode_seed(seed, campaign_id, iteration)` on CUDA, tight tolerance (bitwise explicitly NOT required). **④ fidelity standing**: winner coordinates must pass the fidelity manifest. Direction-splitting pitfall handled: minimal pairs occur in both record orders (cross-seed/family), so attribution fragments one logical effect into direction-split keys — locks merge via `_canonical`/`_merged_attributions`; the report's pooled table now canonicalizes too (with summed pair counts). **Registered state locked against live artifacts**: the R5b-B records satisfy both null locks at the 0.05 claimable floor — if a repair or re-commission flips them, re-evaluate, never silently preserve. Engineered-ground-truth tests prove the locks have teeth (replicating/flipped/absent/stable/unstable cases). ③ is CUDA-gated (skip without GPU). |
| R5b-E (golden manifests) | ✅ **Verified 2026-08-31 (rev d)** — every commissioned campaign writes `records/manifest.json` with git commit, config sha256, uv.lock sha256, torch/CUDA versions, device_requested, determinism flags (construction_seeding, replay_mode=tolerance), seeds, tasks, budget, fidelity-manifest source+sha256, replication summary. Figure regeneration = the commissioning scripts + `scripts/fidelity_gate_report.py` (checked in). R5.2 closes with this: the one-command campaign→report flow is documented in `autoscientist_campaigns/README.md` + Quick Commands below. |

## 🔁 What Changed in This Revision

1. **GPU-first is the critical path, not an optimization.** CPU settle paths stall campaigns (P5 model sweep stalled; GPU directive 2026-08-31). R1 precedes everything.
2. **Campaign commissioning promoted to headline deliverable.** The P5 stack is built but `autoscientist_campaigns/` is empty — zero completed runs (RESEARCH3 PR-9). Demonstrability = one commissioned run.
3. **Five scattered "improvement opportunities" sections folded into R2–R4** — nothing lost; resolved items dropped.
4. **Old P6 (Research Phases 4/5/6) absorbed by RESEARCH3's critical paths.** This doc ends where RESEARCH3 begins; R6 is the handoff.
5. **Sequencing:** R1 → R5.1a (CPU smoke, **not** GPU-gated) → R5.1b/c; R2/R4 interleave; R3 is pull-based via campaign **capability manifests** — a defect touching a manifest value blocks that campaign's *interpretation*, not its execution (R5b-0 gate enforces); R6 last.
6. **Bitwise determinism deprioritized:** discovery/replay locks use tolerance + environment-locked manifests on GPU; bitwise equality is an opt-in extra (CPU reference or explicit deterministic mode), never a requirement.
7. **U-axis ownership enforced (2026-08-31):** codebase-wide optimizer sweep — external torch optimizers may no longer drive composed Systems; audit table below documents the fixed and the legitimately-external sites.

---

## 🔍 U-Axis Bypass Audit (2026-08-31, complete)

**Rule:** a composed System (`compose_system`/`compose_joint_system`/native factory) is only ever
updated through its own ParameterUpdate axis. Custom-loss harnesses call
`core.pipeline.apply_autograd_update(system)` (autograd grads → pseudo-grads → `update.step` →
`geometry.update_params`); plain-`nn.Module` baselines are out of scope.

**Fixed this session (violations — external optimizer drove a composed System):**

| Site | Was | Now |
|------|-----|-----|
| `validation/tracks/tradeoff_tracks.py` [57a/57b] | `optim.Adam` + `loss.backward()` loop on native EqProp/Backprop systems; "EqProp" arm never settled (D/C axes dead) | `train_and_measure` drives `model.train_step` (full 5-axis pipeline) |
| `validation/tracks/hardware_tracks.py` [18a] | `create_optimizer(sgd)` + `optimizer.step()` on native EqProp | `apply_autograd_update(model)` |
| `validation/tracks/application_tracks.py` [21c] EWC | `create_optimizer(adam)` + manual EWC penalty + `optimizer.step()` on native EqProp | EWC penalty loss → autograd → `apply_autograd_update(model)` (canonical route remains an `ElasticConsolidationUpdate` coordinate — future switch) |
| `validation/tracks/core_tracks.py` `_train_model`/`_train_model_sn` | `create_optimizer` created, never stepped (dead stray) | deleted |

Also removed: 8 silently-ignored `use_spectral_norm`/`max_steps` factory kwargs (see R3.9 note —
those "SN ablation" tracks compared identical models; the comparison signal was fake).

**Audited legitimate (no action; listed so future sweeps don't re-litigate):**

| Site | Why it's not a violation |
|------|--------------------------|
| `experiments/joint/*` (`PlasticityModel`, `PlasticityModulatedModel` + `torch.optim.Adam`) | benchmark harness models, not composed Systems; no U-axis exists to bypass. Optimizer-phase hygiene (rebuild Adam between meta-train and ψ-adaptation) = RESEARCH3 PR-1 |
| `ontology/system.py` `_AdaptedSystem.train_step` fallback SGD | Strangler-Fig seam: legacy `nn.Module` models can't be driven by the ontology update; System `EuclideanUpdate` supplies `step_size` |
| `core/trainer.py` `dispatch_train_step` | BPTT fallback requires an optimizer only when the model has no learning rule; System models take the `train_step` path |
| `core/local_learning/**` (`BioOptimizer` etc.) | legacy model layer; the optimizer *is* the learning-rule implementation |
| `zoo/**` | ✅ deleted 2026-08-31 (R2.1) — historical entry, no longer applicable |
| `training/rl.py`, `sklearn_interface.py`, `graph/training.py`, `domains/trainer.py`, `benchmarks/rigorous.py`, `benchmarks/algorithm_migration.py`, `deployment/quantization.py`, `lightning_/module.py`, `core/ebm.py`, `core/dynamics/adapters.py`, `core/nebc.py`, `scripts/z3_reverification_audit.py` | plain `nn.Module` baselines / infra / distillation targets; no composed System in the loop |

---

## 🎯 R1 — GPU-First Runners + Close P5 (do first) — ✅ COMPLETE

*Verified: `create_native_*` factories had no `device` param (`**kwargs` silently ignored → tensors always construct on CPU even under CUDA trainers); `autoscientist/campaign.py:717`, `core/system_trainer/factory.py` + `joint.py` hardcoded `device="cpu"`. Additionally discovered: `_ComposedSystem.to()` moved params into a throwaway dict view (no-op) — fixed.*

| # | Task | Status |
|---|------|--------|
| 1.1 | `device` through native factories | ✅ explicit `device: str \| torch.device = "cpu"` on all factories via `compose_system(device=…)`; `**kwargs` removed → unknown kwargs raise `TypeError` |
| 1.2 | Auto-device in runners | ✅ `get_device()` (single resolver, `core/utils/device.py`) in `AutoScientist._execute_proposal`, `CampaignStack(device="auto")` + checkpoint restore, `evaluate_episode` (batches follow the joint's parameter device), `evaluate_migration` (default `"auto"`), joint suites |
| 1.3 | CUDA placement guard | ✅ `tests/property/test_native_device_placement.py` — all 28 factories + buffers + substrate-metadata agreement + kwargs-rejection + CPU default |
| 1.4 | Suite-wide construction seeding | ✅ `construction_seed()` helper applied to parity classes (`test_ontology_parity.py`): 4 presets-vs-native pairs seed identically per construction (the P3 CUDA flake pattern), credit composition seeded before compose. Crash-freedom-only tests (`acc >= 0.0`) left unseeded — no threshold to flake |
| 1.5 | Close P5: pyright policy | ✅ `pyrightconfig.json`: basic repo-wide, elevated-standard on `computronium/ontology` (0 errors); pre-commit hook gated. Note: pyright's `strict` array cannot be downgraded per-rule, so full `strict` on ontology (131 findings, mostly torch `Unknown` tracking) is deferred — see improvement opportunities |

**Done when:** ✅ every factory accepts explicit `device` and rejects unknown kwargs · ✅ placement guard green over all 28 factories · ✅ EqProp single-epoch MNIST on CUDA ≈ 5.6 s (256-batch, 20 settle steps) · ✅ runners default to CUDA when available · ✅ pyright policy enforced in pre-commit · ✅ parity classes construction-seeded (R1.4)

## 🧹 R2 — Retirement & Signal Honesty (stability; interleavable, never blocks R5)

| # | Task | Detail |
|---|------|--------|
| 2.1 | Zoo retirement | ✅ **Done 2026-08-31** — `computronium/zoo/**` deleted; mep/TileLM/deployments/model_spec extracted first-class, dead modules + tests removed, 14 consumer modules + 8 test files re-pointed (see Completed Record). Full-suite green before and after |
| 2.2 | Dead/duplicate sweep | **Partially done (2026-08-31):** `ontology/dynamics/primitives.py` deleted, `ontology/utils/state.py` deleted, dead state helpers + duplicate helper copies removed; the zoo half of the legacy/new parallel-pairs class is now gone by deletion (R2.1). Remaining: `Substrate` naming (`ontology/_substrate.py` impl vs `ontology/substrate/` facade — facade is fine, consider merge); grep for other parallel legacy/new pairs |
| 2.3 | Registry API unification | ✅ **Done 2026-08-31** — `list_models()` delegates to `Registry.list()` (aliases + lazy-trigger consistent); `get_metadata` already resolves aliases via `resolve_alias`; boundary probe hardened (see Completed Record) |
| 2.4 | xpass noise fix | ✅ **Done 2026-08-31** — native smoke tile tests split into crash-free strict smoke + strict-xfail learning-capability locks (see Completed Record). Gate 0 xpassed. Discovery-lock prereq satisfied |
| 2.5 | Skip census (one pass, then done) | ✅ **Done 2026-08-31** — dead legacy deleted, adapter-bug skips→strict xfails, stale Riemannian restrictions lifted (tests now pass), residuals categorized (see Completed Record). Skip count now meaningful: gate 66→54 |
| 2.6 | Small items | ✅ **Done 2026-08-31** — `uv add kademlia`; grpc-seam stub folded into the working subprocess pattern (dead scaffolding deleted); axis_probe `[2-0]` flake: no recurrence this session |

## 🧩 R3 — Capability Completeness (pull-based via campaign capability manifests)

**Blocking rule (supersedes "interleavable, never blocks"):** each commissioned campaign declares a
**capability manifest** — the set of axis values it uses. Any R3 defect touching a value in that
manifest **blocks that campaign's interpretation** until fixed or explicitly quarantined (the R5b-0
fidelity gate is the enforcement mechanism: failing values are excluded and listed, and the manifest
is recorded in the fidelity report). R3 items outside the manifest remain non-blocking. This keeps
R5b runnable while preventing interpretation on top of a known-broken axis, and gives R3 a
pull-based priority order driven by what the science actually needs. Current R5.1c manifest
verdicts (see `r51c/records/fidelity_report.md`, re-run 2026-08-31 after imp-24): **48/72 pass,
480/720 records survive**; quarantined = 12 instantaneous×thermodynamic_contrast (R3.9-fenced,
uncomposable) + 12 instantaneous×local_goodness (structural zero contrast — honest fail). The
earlier verdicts (local_goodness stub, plasticity unwired, accuracy leakage, inert FA) are all
repaired — history in the Completed Record and `git log`.

| # | Blocker | Fix shape |
|---|---------|-----------|
| 3.1 | ✅ **Fixed 2026-08-31** — `DiffusionDynamics.settle` seeds fresh `requires_grad_` leaves per Langevin step and runs its internal `autograd.grad` under `torch.enable_grad()`; diffusion integration tests un-xfailed (4/4). ✅ Dynamics probes added 2026-08-31 (imp-24) — all five dynamics values have real verdicts (no "blocked" remains on the joint grid/full space) |
| 3.2 | ✅ **Resolved 2026-08-31 (imp-24)** — `instantaneous × thermodynamic_contrast` declared conceptually invalid (contrast requires settling) and fenced at composition (`IncompatibleCoordinateError`); `random_projections` repaired to true FA (autograd top error through fixed random feedback matrices) so the pairing is live and the flagship FA factory learns again. FA-on-tile weights break the layered contract → honest zeros (tile non-learning already pinned) | — |
| 3.3 | ✅ **Fixed 2026-08-31 (imp-21)** — all four credit bodies implemented (LocalGoodness/TemporalTrace/TargetInversion/Homeostatic); fidelity credit probe passes for all 7 credit values on their valid pairings. ✅ recurrent×local_goodness under EM repaired 2026-08-31 (imp-24: count-guard, not graph) |
| 3.4 | Tile × dynamics matrix (tile_ep/pc/gnn/snn device-dynamics incompatibility; tile_fa/tp/hebbian) | Fix or document as permanent xfail with precise reasons |
| 3.5 | `_AdaptedSystem._infer_geometry` hardcoded (784→256,128→10) | Recover heuristics from deleted `adapter/` package |
| 3.6 | `_TaskTrainer` gaps | Scheduler wiring, energy tracking, honor `tracker`/`safety_config` (wire when hyperopt trials need them) |
| 3.7 | Substrate fidelity nits | Neuromorphic: real spike dropout or drop the cosmetic `sparsity` field; Memristive: restore conductance-range semantics (pairs with RESEARCH3 substrate work) |
| 3.8 | Stretch | `natural_language_query` TF-IDF weighting; derive `V_nudged = free energy + β·loss` to strengthen the predictive-coding Lyapunov xfail |
| 3.9 | Coordinate validity matrix | ✅ **Operating 2026-08-31 (imp-24)** — first fence raised live: `instantaneous × thermodynamic_contrast` (contrastive credit needs target-responsive settle). Matrix so far: EM × all 7 credits valid; instantaneous × {FA, local_goodness, temporal_trace, target_inversion, homeostatic, gradient} valid (FA/local_goodness/temporal_trace/gradient verified live or honestly quarantined by the gate); instantaneous × thermodynamic_contrast invalid; tile_mesh × layered dynamics invalid. Remaining: enumerate supported/unsupported combos beyond D×C as campaigns pull them |

## ⚙️ R4 — Substrate API Breadth (P4 continuation; opportunistic — pulled by campaign needs)

| # | Task |
|---|------|
| 4.1 | FA feedback projection through the Substrate operator API (validates non-settle paths) |
| 4.2 | Register `SubstrateSettleKernel` in `KernelRegistry` for the EQPROP family (currently `EqPropKernelBackend` covers all targets) |
| 4.3 | MEP Triton kernels (Muon, Fisher whitening) → Substrate update operator |
| 4.4 | Sparse substrate transpose-mask handling; ternary `init_scale` param (un-xfail ternary equivalence); optional per-step `inject_state_noise` during settle for analog/memristive substrates |

## 🎬 R5 — Demonstrable Product Surface (RESEARCH3 PR-9)

| # | Task | Detail |
|---|------|--------|
| 5.1a | ✅ CPU smoke campaign | **Commissioned 2026-08-31** — `autoscientist_campaigns/smoke_cpu/`: 2–5 coords/iter from `joint_smoke`, seed 0, synthetic task (8-dim, batch 16). Lifecycle: start → checkpoint (ENTERING-episode, interval 1) → **mid-flight SIGKILL at 1 durable episode** (iteration 0 in DB) → CLI `--resume` → completed through iteration 6 (13 episodes; interrupted iteration 1 re-ran its deterministic coordinate stream → documented overlap). Manifest + report + logs + YAML checkpoint in `records/`. Regenerate: `uv run scripts/commission_smoke_campaign.py --fresh` |
| 5.1b | ✅ GPU quick campaign | **Commissioned 2026-08-31** — 3 seeds in `quick_gpu{,_seed1,_seed2}` (campaign-ids `quick_r51b_s*`), 4 coords/iter × 8 iters, **33 episodes each**, kill→resume intact, manifest `device_requested=cuda`, metrics/resources recorded, frontier + counterfactual + replication rendered. Placement guard green; **no speedup at smoke scale** (launch-bound toy task — measured, documented in README + Completed Record); speedup evidence = training-scale (EqProp MNIST ≈ 5.6 s/epoch CUDA). Improvement-10 note: iteration 1 re-ran post-kill, duplicating recorded coordinates (uniqueness fix still open) |
| 5.1c | ✅ **Commissioned campaign** | **Done 2026-08-31** — 72 coords × 5 seeds × 2 families, 720 episodes, 72/72 replicated, kill→resume with zero duplicates, golden manifest + report + `episodes.json` in `autoscientist_campaigns/r51c/`. Script extended for multi-seed + multi-family in one campaign dir (per-seed stores, shared grid, family-aware batches). Task-scale caveat: smoke-scale episodes are launch-bound (improvement-14) — compute-efficiency contrasts need task-scale variation. Next: R5b discovery locks (prereqs cleared) |
| 5.2 | ✅ One-command demo on GPU | **Done 2026-08-31 (rev d).** `comp campaign run` / commissioning scripts → Pareto + replication + counterfactual report end-to-end on CUDA, documented in `autoscientist_campaigns/README.md` + Quick Commands. R5b-C upgraded the report with the defect-filtered Pareto over 𝒞 and knee attribution. |
| 5.3 | ✅ Demo/quickstart truth-check | **Done 2026-08-31** — quickstart verified end-to-end on CUDA (93.3%/92.9%), dead helpers deleted, `QUICKSTART_EPOCHS` knob, slow-tier smoke `tests/slow/test_quickstart_smoke.py` added; `demo/main.py` compiles vs current API (campaign wiring still zero — R5b-F Stage 2) |
| 5.4 | ✅ CLI/docs polish | **Done 2026-08-31** — prog-name `biopl`→`comp` across CLI adapters, docstring entry-points normalized, README pyright claim aligned |
| 5.5 | ✅ Export pin | **Done 2026-08-31** — `tests/integration/test_pt2_export_roundtrip.py`: PT2 round-trip bitwise-equal for Feedforward + Recurrent geometry modules |

### R5b — Discovery Demo Package (the "prove it" milestone)

*Demonstrate the ontology flywheel making a discovery — end to end on Digital (CPU/GPU) substrate, every claim locked in by tests. No hardware needed: the D×C×U×M axes carry the discovery space on CPU/GPU (momentum vs plain EqProp settle convergence, spectral/Riemannian U-axis stabilizers, FastWeight/Routing adaptation vs Null, ternary/sparse substrate trade-offs). Smoke-scale L1 runs are feasible on CPU today; GPU (R1) makes sweeps credible.*

| # | Item | Detail |
|---|------|--------|
| 0 | ✅ **Implementation-Fidelity Gate** | **Done 2026-08-31.** Before any campaign result is interpreted, every axis *value* in the grid must pass a per-axis fidelity check asserting it behaves per spec: EnergyMinimization actually settles (energy descends, monotone, target-responsive); Instantaneous is a single pass and does not inadvertently settle; each CreditAssignment produces non-empty pseudo-gradients; each ParameterUpdate moves parameters on `train_step` given signal; each plasticity mutates / doesn't-mutate ψ as specified. Coordinates failing fidelity are **excluded from attribution and listed, not silently dropped** — a test suite over the same grid the campaign uses. Implemented: `core/campaign/fidelity.py` (probes + `fidelity_manifest` + `defect_filtered_attribution`), `tests/property/test_campaign_fidelity.py`, report script `scripts/fidelity_gate_report.py`. First application: the R5.1c retrospective audit (Completed Record) — **8/72 coordinates pass; 64 quarantined** |
| A | ✅ **Pre-register one toy-scale hypothesis** | **Done 2026-08-31.** `configs/preregistrations/adaptation_efficiency_maxis_48coord.json` — RESEARCH3 L1 Adaptation Efficiency hypothesis (FastWeight/Routing ≥30% adaptation step reduction vs Null at equal accuracy) on the 48-coordinate fidelity manifest. Includes: (a) fidelity preconditions for all axes per arm, (b) broken-instrument kill criterion (ψ non-mutation / zero modulate sensitivity → INCONCLUSIVE, not null), (c) fidelity failure = inconclusive statement. DECISIONS.md entry logged per E-11. |
| B | ✅ **Locked grid campaign on GPU** | **Done 2026-08-31; re-commissioned 2026-08-31 (rev d) after imp-17 + task-rotation repairs.** `autoscientist_campaigns/r5b_b/`: 48 fidelity-passing coords × 5 seeds × 2 task families × 2 visits = 480 episodes, kill→resume on seed 0 verified (96 episodes, zero duplicates). Golden manifest + report + episodes.json. Post-repair state: replication gate **48/48** (parity rotation fixed), wired resource vectors, Pareto 25 frontier coords / hypervolume 412. Fidelity gate: 48/48 pass, 480/480 survive. |
| C | ✅ **Defect-filtered evidence chain** | **Done 2026-08-31 (rev d).** Pareto frontier over 𝒞 (task_loss ↓, compute/memory/energy/latency/ψ-capacity ↓) + counterfactual attribution naming the axis that owns each knee, computed *only* over episodes whose coordinates pass the fidelity gate, stratified per (seed, family) per improvement-18 — rendered by `scripts/fidelity_gate_report.py`. Required imp-17 (resource accounting was latency-only → frontier collapse) + the task-rotation parity fix. Re-commissioned R5b-B evidence: **25 frontier coordinates, hypervolume 412, pooled top plasticity fast_weights→null −0.0243, stratified unstable — no claimable effect** (registered null). |
| D | ✅ **Discovery locks (tests)** | **Done 2026-08-31 (rev d).** `core/campaign/discovery.py` + `tests/property/test_discovery_locks.py`: ① winner-must-replicate (per-stratum gap ≥ threshold; null-inverted), ② attribution rank (pooled + per-stratum, defect-filtered), ③ GPU replay from `episode_seed` (CUDA-gated, tolerance not bitwise), ④ implementation-fidelity standing (winner coords must pass the gate). Engineered-ground-truth cases prove teeth; the registered null result is locked against the live R5b-B artifacts and flips on repair/re-commission (forces re-evaluation, never silent preservation). |
| E | ✅ **Golden manifest (mandatory from R5.1c on)** | ✅ **Verified 2026-08-31 (rev d).** Every commissioned campaign writes git commit, config hash, dependency lock, torch/CUDA versions, device, deterministic mode, seed list, task family, budget, replication summary → `records/manifest.json`; figure regeneration = the checked-in commissioning scripts + `scripts/fidelity_gate_report.py` (RESEARCH3 E-3) |
| F | Discovery Report UI | Stage 1: static HTML/JSON report from the stack (frontier, replication, attribution, timeline) — snapshot-tested. Stage 2: live Campaign tab in `demo/main.py` (719-line NiceGUI app, currently **zero** campaign/autoscientist wiring). **Next execution item.** |

**Ordering (fidelity-gated):**

```
R5b-0 fidelity gate (test suite over the grid)        ✅ done 2026-08-31
→ retrospective audit of R5.1c deltas                  ✅ done 2026-08-31
→ instrument repairs imp-20 → imp-21 (+R3.1) → imp-22; gate re-run each   ✅ done 2026-08-31 — manifest 8/72 → **30/72** (= Execution Order 8)
→ R3.2/R3.9 reclassification + dynamics-probe coverage                     ✅ done 2026-08-31 (imp-24) — manifest 30/72 → **48/72** (= Execution Order 9)
→ R5b-A pre-register (theory-driven, broken-instrument kill criterion; 48-coordinate manifest)   ✅ done 2026-08-31 (= Execution Order 10)
→ R5b-B locked campaign (restricted to fidelity-passing coordinates)     ✅ done 2026-08-31 (= Execution Order 11)
→ R5b-C defect-filtered evidence chain   ✅ done 2026-08-31 (rev d) — imp-17 resource wiring + task-rotation fix + re-commission; frontier 25 coords / hypervolume 412; top attribution stratified-unstable
→ R5b-D locks (①②③④)  ✅ done 2026-08-31 (rev d) — discovery.py + 17 lock tests, registered null locked against live artifacts
→ R5b-F Stage 1 report UI   (next execution item)
```

R5b-C findings (re-commissioned R5b-B, rev d): Defect-filtered attribution over 480 surviving episodes — canonical top is plasticity (fast_weights→null −0.0243), then dynamics (−0.0173) and credit (−0.0147), but stratified per (seed, family) the signs flip (dynamics ±0.2188 across parity strata) — **no claimable effect; registered null**. The earlier pooled deltas (+0.083 credit, +0.051 plasticity) shrank once the defective-instrument era fully washed out of the data. Replication gate **48/48** (was 0/48 — parity rotation fixed). Hypervolume semantics: a data-derived reference with anti-correlated objectives yields small/zero *dominated* volume even with a rich frontier — the frontier size + knee ownership table is the evidence, not the hypervolume scalar alone.

R5b still runs; it just can't *claim* anything until the instrument is validated. A gate is added, not a blocker.

**Honesty ladder:** rung 1 = flywheel completes with evidence chain (any outcome demoable — nulls are results); rung 2 = validation discovery (rediscovers known non-trivial structure, attributed); rung 3 = novel discovery (RESEARCH3 Algorithm Discovery item, kill-criterion governed — never promised on a date).

## 🔬 R6 — Handoff to RESEARCH3 (old P6 absorbed)

Old P6 phases map onto RESEARCH3's critical paths — execute there, not here:

| Old P6 item | RESEARCH3 home |
|-------------|----------------|
| Phase 4: Regime Discovery (Bandit Router, Memristive IR-Drop sweep, Photonic Epistemology Swap) | AutoScientist campaign types (substrate ablation, epistemology swap) |
| Phase 5: Family-Coverage Benchmark (coordinate lock ≥30, Resource-Vector Runner, Dynamical Phylogeny) | De Facto Non-Backprop Benchmark + PR-3a |
| Phase 6: Frontier Certification (M-Axis Frontier, Goldilocks Map, Manifesto Dataset) | CP-A tail: frontier campaign + failure manifesto |

**First moves after R1** (RESEARCH3 startup sequence): PR-1 optimizer-phase hygiene (rebuild Adam between meta-train and ψ-adaptation — momentum carry-over contaminates the Δθ=0 claim) → PR-2 θ-invariance audit harness → PR-3a resource instrumentation → PR-7 shakedown (L3.5 → L1 → L2/L3). `docs/baseline.md` (PR-0) and `docs/evaluation_fairness_contract.md` (PR-6) already exist.

---

## 🗓 Execution Order (sessions 1–7 done; 8+ pinned)

1. ~~**Close P5** — `pyrightconfig.json` + pre-commit hook~~ ✅ done 2026-08-31
2. ~~**R1 device threading** — factories, kwargs rejection, placement guard, runner auto-device~~ ✅ done 2026-08-31
3. ~~**R1 validation** — construction seeding~~ ✅ 1.4 done 2026-08-31 (parity classes seeded; parity file 30 pass) · ✅ EqProp MNIST epoch 5.6 s on CUDA · ✅ zero silent CPU fallback (placement guard)
4. ~~**R5.1a CPU smoke campaign**~~ ✅ commissioned 2026-08-31 — start → checkpoint → **mid-flight kill** → resume → complete → artifacts (`autoscientist_campaigns/smoke_cpu/`)
5. ~~**R5.1b GPU quick campaign**~~ ✅ commissioned 2026-08-31 — 3 seeds, 33 episodes each, CUDA placement verified, lifecycle intact; smoke-scale speedup honestly measured as absent (launch-bound toy task) — see Completed Record
6. ~~**R2 signal honesty** — xpass split, skip census, zoo extraction list~~ ✅ done 2026-08-31 (R2.4/2.5/2.6 complete; R2.1/2.3 complete 2026-08-31 — see Completed Record)
7. ~~**R5.1c commissioned campaign**~~ ✅ done 2026-08-31 — 72 coords × 5 seeds × 2 families, 720 episodes, 72/72 replicated, kill→resume zero-duplicate, golden manifest in `autoscientist_campaigns/r51c/` (script extension: multi-seed + multi-family per campaign dir, grid layout, family-aware batches)

8. ~~**Instrument repairs — the manifest-widening batch**~~ ✅ **done 2026-08-31** (a) imp-20 free-forward metric + leakage lock; (b) imp-21 credit bodies (10/72 after step b); (c) R3.1 diffusion autograd (enable_grad + leaf re-seeding); (d) imp-22 ψ wiring + modulate hooks + rule_state input projection (**30/72**, 300/720 records survive). Gate green throughout; pyright ontology 0 errors. Full detail in the Completed Record. Residual manifest gaps → item 9
9. ✅ **R3.2/R3.9 reclassification + probe coverage** — **done 2026-08-31 (imp-24, see Completed Record)**: (i) instantaneous×contrastive classified — thermodynamic_contrast pairing fenced at composition, random_projections repaired to true FA; (ii) dynamics probes for predictive_settling/spike_integration/diffusion (real verdicts — spike passes, predictive/diffusion nudge-unwired = fail with repair shape); (iii) recurrent×local_goodness under EM repaired (count-guard, not graph — the "graph not preserved" note was a misdiagnosis); (iv) surrogate-alignment locks un-xfailed via pipeline-mediated re-settling FD (LocalGoodness cos ≈ 1.0; TargetInversion 0.95-0.99 approximate-by-construction, tol 0.90); (v) M-probe upgraded — ψ non-const + modulate sensitivity, immediately caught the missing-y wiring bug. **Manifest 30/72 → 48/72**
10. ✅ **R5b-A pre-registration** — **done 2026-08-31**. `configs/preregistrations/adaptation_efficiency_maxis_48coord.json` (RESEARCH3 L1: FastWeight/Routing ≥30% adaptation reduction vs Null at equal accuracy, on 48-coordinate fidelity manifest). Broken-instrument kill criterion + fidelity-failure=inconclusive statement included. DECISIONS.md logged. The post-repair ±0.8 dynamics delta was NOT pre-registered (explicitly declared unclaimed per protocol — observed deltas without pre-registration are never interpreted).
11. ✅ **R5b-B locked campaign** — **done 2026-08-31; re-commissioned (rev d) after imp-17 + task-rotation repairs — see item 13.** `autoscientist_campaigns/r5b_b/`: 48 fidelity-passing coords × 5 seeds × 2 task families × 2 visits = 480 episodes, kill→resume on seed 0 verified (96 episodes, zero duplicates). Golden manifest + report + episodes.json. Fidelity gate: 48/48 pass, 480/480 survive. Counterfactual attribution works across coordinates.

Then R5b-C/D as specified (R5b Ordering) — next execution items. ~~Zoo deletion (R2.1)~~ ✅ done 2026-08-31; kernel breadth (R4) stays non-blocking.

12. ✅ **R2.1 zoo retirement + R2.3 registry unification** — **done 2026-08-31** (this session). `computronium/zoo/**` deleted after first-class extraction (mep → `computronium/mep`, TileLM + deployments → `computronium/models/`, model-spec helpers → `computronium/core/model_spec.py`); dead/superseded zoo modules (propagators, sparsity, utils, nebc_base, duplicate optimizer wrappers, superseded tile models) + 4 dead test files + broken `scripts/inventory_eqprop.py` removed; 14 consumer modules + 8 test files re-pointed; `list_models()` → `Registry.list()` delegation with hardened boundary probe. Full gate green after: 1219 passed / 0 failed / 33 xfailed / 0 xpassed

13. ✅ **R5b-C evidence chain + R5b-D discovery locks** — **done 2026-08-31 (rev d, this session)**. (i) imp-17 resource accounting wired into `evaluate_episode` (`_episode_resources`: MAC proxy × phases × settle steps, param memory, `free_energy`, ψ-capacity from plasticity config); (ii) task-rotation parity fix (visit-count family alternation, resume-safe; dedup key `(iteration, coordinate)`); (iii) defect-filtered Pareto over 𝒞 + knee ownership + canonical (direction-merged) attribution in `scripts/fidelity_gate_report.py`; (iv) `core/campaign/discovery.py` — `DiscoverySpec` + the four locks; (v) `tests/property/test_discovery_locks.py` (engineered ground truth + GPU replay + fidelity standing + registered-null lock on live artifacts); (vi) R5b-B re-commissioned with the repaired instruments: 480 episodes, kill→resume intact, **48/48 replicated**, frontier 25 coords / hypervolume 412, top effect stratified-unstable → registered null. Gate: 1237 passed / 0 failed / 0 xpassed; pyright ontology 0 errors; ruff net-zero new diagnostics (pareto refactor: `objective_vector` table-dispatch + `_dominated_mask` extraction cut `pareto_frontier` complexity 28 → 14)

Then R5b-F Stage 1 (static report UI, snapshot-tested) — next execution item. R3.4–3.8 and R4 stay pull-based; R6 handoff per RESEARCH3.

## ⚠️ Risks

| Risk | Mitigation |
|------|------------|
| GPU-first exposes numerical flakiness (convergence, tolerances, xfail boundaries shift on CUDA) | CPU-equivalence and GPU-tolerance tests kept separate; construction seeding; device recorded in every test/report |
| Stack mechanically complete but scientifically empty | R5.1 must produce artifacts; thresholds pre-registered (R5b-A); replication gate non-bypassable |
| Discovery locks brittle — too tight flakes, too loose prove nothing | Pre-registered effect thresholds; multi-seed robust deltas; tolerance + env lock on GPU, never bitwise |
| Zoo deletion breaks hidden registrations | Audit → extract live registrations → full suite → delete → full suite |
| R3 becomes capability creep | Track as xfail/invalid-coordinate unless the commissioned grid needs that coordinate |

## 💡 New Improvement Opportunities (2026-08-31 session)

> **20–22 are the pinned instrument-repair batch** — order, fix shapes, and locks in Execution Order 8.

1. **`_ComposedSystem.to()` was a silent no-op** — it reassigned entries in the `geometry.params` dict view instead of moving the underlying `nn.Module` parameters. Anyone "training on GPU" via `system.to("cuda")` was on CPU. Fixed (delegates to `nn.Module.to`); pinned by the placement guard. *Lesson: parameter-dict views hide mutation bugs — prefer module-level placement.*
2. **`use_spectral_norm` was a fake knob** — accepted-and-ignored by factories, so SN "ablations" ([56b], [1a/1b], negative_results) compared identical models and reported fabricated contrasts. kwargs are now rejected; making those ablations real means composing `SpectralConstrainedUpdate` U-axis coordinates (pairs with R3.9 / MEP Kinetics campaigns).
3. **ψ-device coupling** — `initial_psi` for Routing/FastWeight created CPU tensors regardless of θ device; now derives device from `SystemContext.device`. RuleState already device-aware. When adding plasticity primitives, always key ψ off `context.device`.
4. **Pyright policy floor** — `strict` on ontology surfaces 131 findings (mostly torch `Unknown` tracking + private-import usage); pyright forbids downgrading rules inside `strict` paths, so the policy uses elevated-standard there (0 errors). Raising to full `strict` = annotation work in `_dynamics`/`geometry`/`update`; repo-wide basic is ~2.5k findings (pre-existing, never gated).
5. **Dead helper consolidation** — `_layer_stack`/`_learnable_weight_names`/`_set_param_name` each had 2–3 live copies across `geometry.py`/`utils/`/`system.py`/`_substrate.py`; consolidated (canonical: `geometry.py` for `_layer_stack`/`_set_param_name`, `utils/params.py` for `_learnable_weight_names`). `_layer_stack` renamed public (`layer_stack`) — it is cross-module API.
6. ~~**R2.4 xpass split still open**~~ ✅ done 2026-08-31 — tile smoke split into strict crash-free pass + strict-xfail learning-capability locks; gate 0 xpassed. See Completed Record.
7. ~~**R1.4 construction seeding**~~ ✅ done — parity classes seeded via `construction_seed()` helper; see Completed Record.
8. **`compute_energy` duplication** — Energy/Spike/Instantaneous/Diffusion dynamics each carry near-identical `compute_energy` bodies over duck-typed states; extract to one `_energy_from_state(state, geometry)` helper next touch.
9. **SIGKILL to `uv` orphans the worker python (2026-08-31)** — killing the `uv` wrapper leaves the `comp` child alive, which kept executing the campaign to completion in the background. Campaign runners must `killpg` the session (`scripts/commission_smoke_campaign.py::_kill_tree`, `start_new_session=True`). Same hazard applies to spot-instance teardown and the gRPC worker launcher.
10. ~~**Resume can duplicate episodes (2026-08-31)**~~ ✅ fixed 2026-08-31 — `run_campaign` now snapshots recorded `(iteration, coordinate, task)` keys on resume and re-proposed slots emit `already_recorded` instead of re-executing; also closes the add-episode/update-iteration crash race. Pinned by `test_resume_skips_already_recorded_episodes`; verified live in R5.1c (seed 0: exactly 144/144 after kill→resume).
11. ~~**θ init draws ride the ambient RNG across resume (2026-08-31)**~~ ✅ fixed 2026-08-31 — per-episode construction seeding derived from `(seed, campaign_id, iteration, coordinate)` via blake2b (`evaluation.episode_seed`), applied with `torch.manual_seed` before composition in `_evaluate`. Same rebuild ⇒ same θ ⇒ same metrics (`test_rebuild_replays_identical_metrics`). Chose construction seeding over checkpoint-θ redo: replay needs no live checkpoint, and the redo path (which restores checkpointed θ) remains the fault-tolerance route.
12. **`resolve_consumption` mis-reflected function factories (2026-08-31, fixed)** — it reflected on `model_cls.__init__`, which for plain function factories resolves to `object.__init__` → spurious `**kwargs` catch-all → hyperopt forwarded stale sampled kwargs (`learning_rate`, `max_steps`, …) into native factories, crashing trials against R1's kwargs rejection. Fixed via `_construction_callable` (reflect on the function itself); hyperopt/phase2 integration tests green. *Lesson: `obj.__init__` reflection lies for functions — pair it with `inspect.isroutine`.*
13. **Skips hid recovered capability (2026-08-31)** — `test_substrate_with_riemannian_update` was skip-marked for "known limitations" that no longer exist; un-skipped, all 5 substrates pass. *Lesson: the census isn't paperwork — stale skips mask fixed capabilities and dead tests mask removed APIs.*
14. **Smoke-scale campaigns cannot demonstrate GPU speedup (2026-08-31)** — 8-dim/batch-16 episodes are kernel-launch bound; measured GPU ≈ CPU or worse per coordinate (recurrent/settle worst at ~0.24×). Any R5.1c/R5b claim involving speedup must be task-scale-qualified (MNIST-scale settle loops, larger batches) or scoped to lifecycle/placement only. Consequence: R5.1c should vary task family *and* scale if compute-efficiency contrasts are wanted from campaign artifacts alone.
15. **Don't mutate the tree while the gate runs (2026-08-31, process note)** — a mid-run `git stash`/`pop` for a ruff HEAD-comparison killed a census pytest run mid-flight (truncated output, no summary). Run comparisons before/after test batches, never concurrently.
16. **`_ComposedSystem` is not an `nn.Module` (2026-08-31)** — torch.export requires a module; the geometry module is the export unit (pinned by `test_pt2_export_roundtrip.py`). If whole-system export is ever wanted, `_ComposedSystem` needs an `nn.Module` facade — currently deliberate (dataclass system, module per axis).
17. ✅ **FIXED (rev d, 2026-08-31)**: Pareto collapse diagnosed as an instrumentation bug — `evaluate_episode` recorded `ResourceUsage(latency=…)` only (energy/compute constant 0) and `stability_score` was saturated (ρ≡1.0 by PR-5 calibration, `lyapunov_local` hardcoded 0.0, settling_time = guard window constant), so the frontier reduced to single-objective task_loss. Wired `_episode_resources` (deterministic MAC proxy, param memory, target-free settled energy, ψ-capacity) + task-rotation fix; re-commissioned R5b-B: 25-coordinate frontier, hypervolume 412, replication 48/48. Residual: the *stability* axis is still non-discriminative in the campaign Pareto (see imp-36) — pull-based fix if a campaign manifest needs stability contrast.
18. ~~**Counterfactual pooling caveat (2026-08-31, R5.1c)**~~ ✅ operationalized 2026-08-31 — `defect_filtered_attribution` (core/campaign/fidelity.py) filters records by fidelity verdict before attributing, and `scripts/fidelity_gate_report.py` adds per-(seed, family) stratified attribution. R5.1c result: the surviving C/U/G deltas do not rank stably across strata — the pooled R5.1c D/M attributions were an artifact of mixing validated and defective coordinates (see R5b-0 audit).
19. **`FrontierRecord.seed` legacy default 42 (2026-08-31, now harmless)** — the field always stamped 42 until `evaluate_episode` grew a `seed` param; hand-built records in analysis code still default to it. If a consumer ever counts seeds over hand-built records, it will read fiction — consider making `seed` required at the FrontierRecord boundary when the next schema break happens.
20. ✅ **FIXED (imp-20, 2026-08-31)**: `run_train_step` adds post-update target-free `free_forward` pass → `free_loss`, `free_energy`, `free_accuracy`; `evaluate_episode` uses `free_accuracy` as `task_accuracy`, legacy `nudged_fit_accuracy` in metadata. Lock test added: `TestMetricHonesty.test_free_accuracy_is_not_supervision_leaked` passes.
21. ✅ **FIXED (imp-21, 2026-08-31)**: Implemented all four credit bodies — LocalGoodness (per-layer goodness autograd), TemporalTrace (rate-STDP), TargetInversion (transpose feedback via `_propagate_targets`), Homeostatic (setpoint scaling; `homeostatic_target` config). Manifest after this step: 10/72 (feedforward/local_goodness added; recurrent/local_goodness fails honestly — EM settle graph not preserved). Axis cert surrogate-alignment xfailed (need shared grad_ctx — rewrite queued, Execution Order 9.iv)
22. ✅ **FIXED (imp-22, 2026-08-31)**: ψ threaded through `run_train_step` (optional `plasticity`/`psi`/`context`; steps once per episode on z.activity={x,y}) + optional `modulate(activations, psi)` hook (routing = per-sample sigmoid gate strength; fast_weights = per-sample additive, width-projected; substrate_coupled/rule_state engagement-only). `JointSystem.train_step` no longer discards `initial_psi`'s result. M-probe verdict flipped to pass; **manifest 30/72**. Engagement surfaced a latent rule_state shape bug (controller expected `operator_dim`-wide input, got raw input dim) — fixed by width projection; see imp-23
23. **Latent shape bugs hide behind unwired code paths (2026-08-31, imp-22)** — `RuleStatePlasticity.step` concatenated raw input against a controller built for a fixed `operator_dim` width; the mismatch never fired because nothing called `step` inside the episode pipeline. The moment imp-22 engaged the path, axis_probe caught it immediately. *Lesson: engaging a dormant path is itself a test surface — wire-first, then probe; expect the probe to find construction-time assumptions baked into unreachable code.* Same class of risk: any primitive whose `step` assumes fixed dims (check substrate_coupled next; it was engagement-verified only)
24. ✅ **FIXED (imp-24, 2026-08-31)**: both alignment locks rewritten pipeline-mediated — the FD closure re-runs the phase settling per perturbation under the shared grad ctx (`_settle_phases_grad`), construction-seeded, CPU reference, support-restricted cosine, convergence early-exit disabled. LocalGoodness cos ≈ 1.0 (un-xfailed at 0.90); TargetInversion 0.95-0.99 (un-xfailed at 0.90 — its pseudo-gradient treats propagated targets as constants, approximate alignment by construction). The old "graph connectivity" xfail reason was wrong; the real failure modes were stale-state FD (cos = 0), convergence-exit discontinuities, and unsupported-zero cosine dilution
25. **Silent dispatch fallbacks run the wrong algorithm (2026-08-31, imp-24)** — `compose_joint_system_from_configs` had `else: InstantaneousDynamics` / `else: EuclideanUpdate` / `else: NullPlasticity` catch-alls; a "diffusion" coordinate **silently executed InstantaneousDynamics** and an "ewc"-typo update would silently run SGD. All three dispatches now raise on unknown values. *Lesson: same class as the fake `use_spectral_norm` kwarg (imp-2) — any silent fallback in a compose path manufactures fake ablation arms. Grep for bare `else:` in dispatch tables before the next axis value lands.*
26. **Flagship factory was an inert instrument (2026-08-31, imp-24)** — `create_fa_mlp`/`create_fa_system` (Instantaneous × RandomProjections) had Δθ = 0.0 **exactly**: the credit's error signal was the free-vs-nudged activation contrast, structurally zero under a target-blind single pass. Every FA "training" run ever executed on the composed path learned nothing while L1 smoke (crash-freedom + finite metrics) stayed green. *Lesson: "trains" (loss ≥ 0, acc ∈ [0,1]) is a crash-freedom contract, not a learning contract — every factory in the README table needs a params-moved lock like the tile learning-capability locks (now exists for FA; the remaining factories should get the same probe on next touch).*
27. **Settle-state staleness: not all settles mutate in place (2026-08-31, imp-24)** — `EnergyMinimizationDynamics.settle` mutates the input `SystemState`; `PredictiveSettling`/`SpikeIntegration`/`Diffusion` return a **new** state via `_create_output_state` and leave the input untouched. Any caller reading `state.activations` after calling `settle(...)` without taking the return value silently gets pre-settle acts (the fidelity probe did — it compared forward-pass logits and "discovered" a non-descent that didn't exist). Fixed in the probe (`_settled_output` reads the returned state). *Lesson: the `settle` protocol has two mutation contracts; document it on the Protocol and make one canonical (in-place + return), or rename the rebuilders.*
28. **FD-vs-autograd alignment: three silent under-verifiers (2026-08-31, imp-24)** — (a) stale-state FD (the objective closes over pre-computed states → FD ≡ 0); (b) support-diluted cosine (FD only perturbs `max_points` elements, so full-vector cosine vs autograd is ~support/full ratio, not alignment); (c) convergence early-exit (a ±eps pair can cross the exit step, making the FD a gradient through a step-count discontinuity). Any future FD lock must re-run the map per perturbation, restrict the cosine to the perturbed support, and disable path-length branches.
29. **Nudge-unwired settle paths, repair shapes (2026-08-31, imp-24)** — `PredictiveSettlingDynamics` ignores `target` in the settle math (target only selects the state slot; PC should clamp the output layer toward it in the nudged phase) AND does not descend its own tracked prediction-error energy (141.4 → 146.0 over 3 steps); `DiffusionDynamics.compute_energy_from_state` is a `‖h‖²` placeholder with no target or geometry term, so nudged-Langevin (EP-style) is unimplementable without wiring both. Both quarantined by the gate with repair-shaped verdicts; coordinates using them pass dynamics only after the clamp/target-term lands (pull-based — repair only when a campaign manifest needs them)
30. **Deployments' `family="tile"` registrations are CLI-orphaned (2026-08-31, R2.1)** — `computronium/models/deployments` registers `conv_tile`/`rl_tile`/`timeseries_tile`/`graph_tile` + per-algorithm variants under `family="tile"`, but `cli/shared.py` `FAMILY_MAP` has no `"tile"` entry (native tile models register `family="equitile"`); only `hyperopt/hyperparameter_metamodel.py` still references `"tile"`. At next touch: fold deployments into `family="equitile"` or drop the metamodel's `"tile"` member — otherwise those registrations are enumerate-only dead weight
31. **A probe that calls the API it probes can self-fulfill (2026-08-31, R2.3)** — the module-boundary test used `list_models()` to assert "nothing registered"; once `list_models()` delegated to `Registry.list()` (which triggers lazy registration), the probe would have *caused* the state it asserts the absence of. Replaced with raw-state observations (`Registry._components` empty + `computronium.models.native` absent from `sys.modules`). *Lesson: boundary/lifecycle probes must observe state, never invoke the mechanism under test.*
32. **`load_weights` dual-format contract (2026-08-31, R2.1)** — accepts both the unified `Checkpoint` TypedDict and raw state-dict files (`_resolve_state_dict`). Edge: a raw dict containing a literal `model_state_dict` key would mis-nest (impossible for real param state dicts; noted for future schema work). The old zoo copy duplicated `load_checkpoint` + unwrap + load — the consolidation removed a third checkpoint-loading path
33. **demo/runner.py references undefined `create_tile_mlp` (pre-existing, line ~286)** — ruff-surfaced during R2.1 comment edits; dead path in the demo, harmless until R5b-F Stage 2 rewrites the demo wiring
34. **Lazy first-touch registration vs Hypothesis deadlines (2026-08-31, R2.3)** — the first Registry read that triggers `_ensure_native_registered` costs ≈1.2 s (import chain), which busts Hypothesis's 200 ms default deadline in whichever property test runs first. Fixed in `test_registry_roundtrip.py` with `deadline=None`; if other registry-touching property tests flake the same way (order-dependent), same one-line fix — do not "fix" by pre-warming imports (that would re-couple light imports)
35. **Objective fallbacks silently manufacture constant dimensions (2026-08-31, rev d)** — `pareto.objective_vector` falls back to `metadata.get(obj, 0.0)` for unknown names; `PARETO_OBJECTIVES` listed `plastic_state_capacity`, which had no extractor, so every record read 0.0. A constant objective gives a zero-span Monte Carlo box ⇒ hypervolume 0 *even with a rich frontier* — the same "collapse" signature as the imp-17 instrumentation gap, one level up. Fixed twice over: added the extractor, and the MC box-volume now treats zero-span dims as vacuous (unit width). *Lesson: imp-25's silent-fallback class extends to objective/extractor tables — a missing key must not read as a constant metric.*
36. **Campaign stability axis is saturated, not broken (2026-08-31, rev d)** — `FrontierRecord.stability_score()` cannot discriminate the campaign grid: the windowed-growth probe reads exactly 1.000 for every stable composed system (PR-5 calibration), `evaluate_episode` stamps `lyapunov_local=0.0` unconditionally, and `settling_time` is the constant guard window. Correct for guard purposes, non-discriminative for Pareto. If a campaign manifest needs stability contrast, wire a cheap per-episode proxy (settle iterations-to-threshold, activation variance, or a real local Lyapunov estimate) — pull-based, pairs with RESEARCH3 PR-3a resource instrumentation.
37. **Latency in the resource vector is wall-clock noise (2026-08-31, rev d)** — the Pareto's latency objective is a single `perf_counter` reading around one train_step (CUDA launch jitter dominates at smoke scale: same coordinate spans ~3.5–13 ms). Honest as a measurement, hostile as a frontier objective. Before any latency claim at task scale: repeated-timing methodology (median of k runs, warm-up excluded) or a deterministic proxy. The other four 𝒞 axes are deterministic proxies and safe to compare.
38. **Minimal-pair attribution is direction-split by construction (2026-08-31, rev d)** — `counterfactual_pairs` pairs records in list order, so one logical axis swap appears as two keys (`null→fast +0.2`, `fast→null −0.2`) with float-noise magnitudes; naive "top attribution" picks whichever direction the record stream happened to emit. Locks merge via `discovery._canonical`/`_merged_attributions`; the report's pooled table canonicalizes with summed pair counts. *Lesson: any consumer of `attribute_axis_effects` that ranks or thresholds must canonicalize first — the raw rows are two halves of one effect.*

---

## 🚫 Explicitly Deferred (unchanged)

| Item | Reason |
|------|--------|
| ConvGeometry / GraphGeometry / AttentionGeometry / 3D Spatial Lattice | Science runs on Feedforward/Recurrent/Tile at MLP scale; geometry-DEFERRED skips stay skips |
| Coverage floor | ~16.8%; opt-in `--cov`; raise after API stabilizes |
| Rocq general-case formalization | CP-B pull-based; diagonal case done with paper proof |
| `test_ontology_parity.py` decomposition | Slow-marked (P3); split fast/slow only if gate iteration speed demands |
| Physical hardware (PR-3b / CP-D) | Latency-gated procurement — starts per RESEARCH3 day-one, not here |

## ✅ Definition of Done

- **R1:** ✅ EqProp MNIST epoch on CUDA in ~5.6 s · ✅ parity classes construction-seeded · `pytest -q` green · pyright policy in pre-commit → **R1 complete**
- **R2:** ✅ `computronium/zoo/**` deleted (R2.1, 2026-08-31) · ✅ no dead stubs or duplicate Substrate registrations (zoo duplicates removed; Substrate facade-merge consideration stays open in R2.2) · ✅ 0 xpass noise · ✅ skip census recorded → **R2 complete except R2.2 residual (Substrate facade-merge consideration)**
- **R3:** ✅ DiffusionDynamics un-xfailed (R3.1, 2026-08-31) · ✅ credit stubs implemented (R3.3 via imp-21) · ✅ instantaneous×contrastive reclassified + fenced, FA repaired live (3.2 via imp-24) · ✅ R3.9 matrix operating (first D×C fence raised) · every remaining xfail has a precise reason (✅ enforced this session) · no hardcoded geometry inference → **3.1/3.2/3.3 done + 3.9 operating; 3.4-3.8 remain (pull-based)**
- **R4:** ≥2 operator families beyond settle through the Substrate API · equivalence test per port
- **R5:** ✅ R5.1a+b commissioned campaigns (kill→interrupt→resume) in `autoscientist_campaigns/` with manifests · ✅ R5.1c commissioned campaign (golden manifest, replication gate) **done 2026-08-31** · ✅ demo/quickstart verified on CUDA · ✅ defect-filtered evidence chain (R5b-C, rev d: Pareto over 𝒞 non-degenerate, knee attribution, replication 48/48) · ✅ discovery locks green (① winner-replication + ② attribution rank + ③ tolerance replay on GPU + **④ implementation-fidelity lock** — registered null state locked against live artifacts) — **R5b-C/D complete (rev d); R5b-F Stage 1 remains**
- **R6:** RESEARCH3 PR-1 + PR-2 merged · PR-7 shakedown green

## 🔧 Quick Commands

```bash
# Gate (default, ~75s): unit+property; slow/benchmark/llm auto-deselected; 60s per-test timeout
uv run pytest -q
# Slow tier (~25min; `tests` arg required — testpaths limits bare pytest to unit+property)
uv run pytest tests -m slow
# Full (~23min): uv run pytest tests -m ""
# Coverage (opt-in): uv run pytest tests --cov=computronium --cov-report=term-missing

# Fast gates (seconds): property locks + registry + boundary
uv run pytest tests/property/test_ontology_locks.py tests/unit/core/test_registry.py \
  tests/unit/core/test_module_boundary.py tests/unit/test_refactor.py -q

# CUDA placement guard (all 28 native factories; skips without CUDA)
uv run pytest tests/property/test_native_device_placement.py -q

# Native smoke / settle protocol / joint benchmarks
uv run pytest tests/property/test_native_smoke.py -v
uv run pytest tests/integration/test_settle_protocol_models.py -q
uv run pytest tests/integration/joint/test_benchmarks.py -v

# Commission a smoke campaign: start → mid-flight kill → resume → artifacts
uv run scripts/commission_smoke_campaign.py --fresh
# GPU quick campaign (3 seeds commissioned; see autoscientist_campaigns/README.md):
uv run scripts/commission_smoke_campaign.py --fresh --device cuda \
  --output-dir autoscientist_campaigns/quick_gpu --campaign-id quick_r51b \
  --experiments-per-iter 4 --iterations-first 6 --iterations-resume 8
# `comp campaign run` accepts --device (default auto) / --seed / --tasks / --layout

# R5.1c commissioned replication campaign (72-coord grid x 5 seeds x 2 families):
uv run scripts/commission_smoke_campaign.py --fresh --device cuda \
  --output-dir autoscientist_campaigns/r51c --campaign-id r51c \
  --space joint_grid --objective pareto_replication --layout grid \
  --seeds 0,1,2,3,4 --tasks synthetic,parity \
  --experiments-per-iter 8 --iterations-first 4 --iterations-resume 18

# Quickstart smoke (slow tier, ~1 min on CUDA via QUICKSTART_EPOCHS=1)
uv run pytest tests/slow/test_quickstart_smoke.py -m slow -q
# PT2 export round-trip pin
uv run pytest tests/integration/test_pt2_export_roundtrip.py -q

# R5b-0 fidelity gate + defect-filtered attribution (incl. Pareto over 𝒞) for a campaign
uv run pytest tests/property/test_campaign_fidelity.py -q
uv run scripts/fidelity_gate_report.py --campaign-dir autoscientist_campaigns/r5b_b

# R5b-B locked campaign re-commission (48 fidelity-passing coords x 5 seeds x 2 families):
uv run scripts/commission_r5b_b_campaign.py --fresh --device cuda

# R5b-D discovery locks (①②④ data-level + artifact-backed null; ③ GPU replay, CUDA-gated):
uv run pytest tests/property/test_discovery_locks.py -q

# Type check (policy: elevated-standard on ontology/, basic elsewhere)
uv run pyright computronium/ontology

# NOTE: sync with `uv sync --extra dev --extra lightning` (plain dev sync removes
#   lightning -> 4 collection errors). Serial only — xdist hangs in this env.
#   kademlia is a main dependency (DHT exercised in slow tier).
```
